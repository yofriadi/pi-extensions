{
	"name": "pi-extensions",
	"private": true,
	"type": "module",
	"version": "0.1.0",
	"scripts": {
		"test": "pnpm -r --if-present test",
		"check": "biome check . && tsc --noEmit -p packages/pi-antigravity-oauth/tsconfig.json && tsc --noEmit -p packages/pi-provider-kiro/tsconfig.json && tsc --noEmit -p packages/pi-toon/tsconfig.json",
		"check:fix": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.5",
		"@types/node": "22.19.19",
		"typescript": "5.9.3",
		"vitest": "3.2.4"
	},
	"engines": {
		"node": ">=22.19.0"
	},
	"packageManager": "pnpm@10.5.2"
}
