## 1. Isolate standalone skill resolution

- [x] 1.1 Update `resolveSelectedSkills` to construct its `DefaultResourceLoader` with extension execution disabled while preserving cwd, agent-root, trust, ordinary skill precedence, collision detection, declaration order, and pre-admission validation.
- [x] 1.2a Add a unit regression that invokes the Herdr extension factory with a discovery-only/unbound runtime and proves the already-active parent completion record is not replaced.
- [x] 1.2b Add a unit regression that calls `resolveSelectedSkills` for a selected project/global skill and proves no extension factory ran and the active parent completion record is unchanged.
- [x] 1.3 Verify selected-skill validation still covers normal, manual-only, project/global, missing, duplicate, ambiguous, and untrusted-project cases without creating launch resources.

## 2. Establish session-bound runtime ownership

- [x] 2.1 Replace factory-time `runtime.pi` publication with an active completion-runtime record (bound API, parent session ID, ownership/generation identity) stored under a NEW `Symbol.for` process-global key that the old slot never touches.
- [x] 2.2 Activate the record only from `session_start`; clear it from `session_shutdown` only when the handler still owns the same record, preserving the replacement runtime during reload/session replacement.
- [x] 2.3 Add an active-runtime selector that rejects absent or session-mismatched APIs, route all five shared-slot reads (`deliverBackgroundMessage`, `wakeParent`, `startDeliveryRetry`, the `startDeliveryRetry` call inside `queuePendingDeliveryWithVerification`, and the retry-pump seed) through it, and delete the old `runtime.pi` slot entirely. `wakeParent` resolves the active record and no-ops when absent or session-mismatched; `startDeliveryRetry` no longer takes an API parameter.
- [x] 2.4 Preserve reload adoption of watchers, queues, leases, barriers, and pending deliveries while ensuring old closures cannot clear or send through a newer session runtime.
- [x] 2.5 Keep the detached (queued, non-blocking) promise shape at the queued-launch-error and queued-resume-error sites, but make each rejection handler terminal: catch the delivery error inside the handler, route that run-ID-keyed failure into `queuePendingDeliveryWithVerification` (passing `sessionFile` and `expectedRunId` so verification applies), and end the chain with `.catch(() => undefined)` so no rejection escapes an un-awaited promise. Make status/recovery notifications best-effort: dropped (not queued) when no active record exists, since they carry no run ID.

## 3. Make inactive delivery recoverable

- [x] 3.1 Add a typed inactive-runtime delivery error/state so no Pi action method or acknowledgement verifier is called before a matching session-bound API exists; detect it BEFORE entering the foreground delivery barrier so a deferred entry never reserves a drain batch's wake slot or runs its dedup under the barrier's internal retry, keeping an in-callback defensive check immediately before `sendMessage`.
- [x] 3.2 Update pending-delivery retry scheduling to defer inactive-runtime entries without incrementing ordinary send attempts or marking them exhausted, re-drive them from `session_start`, and ensure the INITIAL enqueue of a runtime-unavailable failure records `attempts = 0` rather than `1`.
- [x] 3.3 Add a `deferredSince` timestamp and a bounded `DEFERRED_DELIVERY_MAX_MS` deferral budget (one hour, separate from `MAX_PENDING_DELIVERY_ATTEMPTS`); clear `deferredSince` on the first attempt that reaches a matching active runtime so the budget measures one continuous unavailable interval. On reload/session-start re-drive of a deferral-exhausted entry, clear BOTH `exhausted` and `deferredSince` before retrying, so the row leaves the undeliverable count and renders as `awaiting runtime` rather than immediately re-exhausting and flapping. An entry continuously deferred past the budget is marked undeliverable with the cause recorded and is not retried as an ordinary send.
- [x] 3.4 Add a distinct `awaiting runtime` widget state for deferred entries, backed by the modified `status widget includes queued and active work` requirement: deferred entries are counted and labelled separately from both retrying and undeliverable rows, so a deferred delivery is never rendered as `delivery retry 0/1`.
- [x] 3.4a Retire or repurpose `selectCompletionApi` and migrate its direct assertions (`packages/pi-subagent-herdr/test/test.ts`) to the active-record selector, alongside the `__test__` delivery call-site migration in 4.3.
- [x] 3.5 Ensure final shutdown suppresses inactive pending work and prevents later retry timers or wake messages from sending.
- [x] 3.6 Keep the existing delivery barrier, deduplication, wake-before-ack ordering, bounded verification, and exactly-once settlement semantics unchanged for active-runtime sends.

## 4. Regression tests and integration coverage

- [x] 4.1 Add unit tests for discovery-only factory isolation, active session API activation, stale shutdown ownership, inactive delivery deferral, activation retry, session mismatch, deferral-budget exhaustion, `awaiting runtime` widget rendering, and final-shutdown suppression.
- [x] 4.2 Extend delivery tests to assert that inactive delivery makes zero `sendMessage`/`sendUserMessage` calls and does not consume the bounded attempt budget.
- [x] 4.3 Add a test-only activate/reset seam for the active-runtime record, migrate the `__test__` `deliverBackgroundMessage` call sites (in `packages/pi-subagent-herdr/test/delivery-wake-ordering.test.ts` and `packages/pi-subagent-herdr/test/review-fixes.test.ts`) to activate a matching session ID, and reset the record in `beforeEach` so the suite stays order-independent across repeated in-process factory invocations.
- [x] 4.4 Correct the integration harness to load the current `packages/pi-subagent-herdr/src/index.ts` extension. Add a production-like configured-resource regression whose purpose-built fixture package ships BOTH (a) the ordinary skill file the test agent selects (the enumerable evidence, since an extension-only package produces nothing in `getSkills()`) and (b) an extension whose factory would write a marker file if executed. Wire the fixture into the parent via a project `.pi/settings.json` package entry (the mechanism design decision 5 names, NOT the harness's `-ne -e` flag path), so the resolver's `DefaultResourceLoader` discovers it. Prove non-vacuity WITHOUT executing the fixture (which decision 2 forbids): in-process in the test file, against the fixture project directory, assert the fixture's skill appears in the resolver's enumerated `loader.getSkills()` → `{skills, diagnostics}`, AND assert the fixture's marker file was NOT produced (the negative proves both non-vacuity and extension-free isolation). The spawned parent/child run supplies the separate one-`subagent_result` assertion. This integration test guards end-to-end one-result delivery; ownership isolation is guarded by 1.2a/1.2b.
- [x] 4.5 Re-run the existing reload integration and selected-skill permission matrix, including manual-only, denied/ask, path-gated, and external-directory cases.

## 5. Verification and documentation

- [x] 5.1 Run typecheck, lint, the complete unit suite, and focused delivery/resource-runtime tests; repeat timing-sensitive tests to rule out flakes.
- [x] 5.2 Run real Herdr integration tests for selected-skill async completion, reload crossing, queue/barrier behavior, and one-result delivery.
- [x] 5.3 Update `packages/pi-subagent-herdr/README.md`, `packages/pi-subagent-herdr/CHANGELOG.md`, or relevant operational notes with the runtime-lifecycle constraint and recovery behavior, then validate the OpenSpec change strictly.
