#!/usr/bin/env python3
import json
import pathlib

p = pathlib.Path.home() / ".grok" / "version.json"
try:
    version = json.loads(p.read_text()).get("version") or "0.2.16"
except Exception:
    version = "0.2.16"
print(f"xai-grok-workspace/{version}")