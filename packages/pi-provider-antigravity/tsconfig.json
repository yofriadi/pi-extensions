{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"lib": ["ES2022"],
		"noUncheckedIndexedAccess": false
	},
	"include": ["scripts/**/*", "src/**/*", "test/**/*"],
	// The accounts adapter/host are parked until the pi-accounts dependency is
	// importable (no main/exports upstream); restore when that lands.
	"exclude": ["node_modules", "dist", "src/account-adapter.ts", "src/accounts-with-antigravity.ts"]
}
